this is package

package is {
    name: string,
    version: semver,
    description: string,
    main: string,
    preferGlobal: boolean,
    bin: string,
    scripts: {
        *: string
    },
    dependencies: dependency_map,
    devDependencies: dependency_map,
    author: string,
    license: ('MIT', 'BSD', 'GPL') or string
}

dependency_map is {
    *: semver
}

semver is string
